version: '3.8'

services:
  postgres:
    image: postgres:16.9-alpine
    container_name: costify-ts-postgres
    environment:
      POSTGRES_DB: costify_ts
      POSTGRES_USER: costify
      POSTGRES_PASSWORD: costify123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init:/docker-entrypoint-initdb.d
    networks:
      - costify-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U costify -d costify_ts"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local

networks:
  costify-network:
    driver: bridge